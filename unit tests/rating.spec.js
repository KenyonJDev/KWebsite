'use strict'

const Song = require('../modules/Rating')
const mock = require('mock-fs')
const fs = require('fs')

// Constants used in tests
const validRating = [1, 2, 3, 4, 5]
const emptyRating = []
const appropriateComment = 'test comment'

